{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "SuperSlicer: Temporary automation verification",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build-linux-current-release/bin/superslicer",
      "args": ["--automation-api"],
      "envFile": "/tmp/superslicer-automation-session.env",
      "cwd": "${workspaceFolder}"
    }
  ]
}
